import { prisma } from '../config/prismaClient.js';

export default {
    findByEmail(email) {
        return prisma.
    },
    findByUsername(username) {},
    create(userData) {},
    findById(id) {},
    update(id, updateData) {},
    delete(id) {}   
};
